{
  "name": "cartolafc-scouts",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch src --ext ts --ignore dist --delay 1 --exec \"clear && npm run build && npm start\"",
    "build": "tsc",
    "start": "node --enable-source-maps dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^22.13.13",
    "nodemon": "^3.1.9",
    "typescript": "^5.8.2"
  }
}
